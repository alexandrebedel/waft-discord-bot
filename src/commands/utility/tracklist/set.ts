// import type { WAFTCommandInteraction } from "@waft/types";

// async function handler(interaction: WAFTCommandInteraction) {
//   console.log("hello fron handler");
// }

// export default { handler };
